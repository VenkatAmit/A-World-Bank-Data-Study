# Pinned to match apache/airflow:2.8.1 base dependencies
# Do not upgrade without testing — Airflow is sensitive to
# version conflicts in its dependency tree

pandas==2.1.4
psycopg2-binary==2.9.9
numpy==1.26.4
sqlalchemy==1.4.51
